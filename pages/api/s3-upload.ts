export { APIRoute as default } from 'next-s3-upload'

// import { APIRoute } from 'next-s3-upload'

// const date = () => {
//   const d = new Date()
//   return `${d.getFullYear()}-${d.getMonth() + 1}-${d.getDate()}`
// }

// export default APIRoute.configure({
//   key(req, filename) {
//     return `${date}/${filename.toUpperCase()}`
//   },
// })
